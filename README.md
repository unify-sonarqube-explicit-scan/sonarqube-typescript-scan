# TypeScript Code Smells Demo

This repo demonstrates common TypeScript/JavaScript code issues:

- Use of `==` instead of `===`
- Lexicographic array sorting
- Unnecessary `await`
- Use of `continue` (often discouraged)

## Usage

```bash
npm install
npm run build
npm start
